# GELİŞTİRME / TEST kurulumu = yalın liste + test araçları.
# Testleri çalıştırmak için:  pip install -r requirements-dev.txt  &&  pytest
-r requirements.txt
httpx>=0.27                # FastAPI TestClient (testler)
pytest>=8.0
pytest-cov>=5.0
